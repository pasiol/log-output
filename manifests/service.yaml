apiVersion: v1
kind: Service
metadata:
  name: log-output-svc
  namespace: applications
spec:
  type: ClusterIP
  selector:
    app: log-output-dep
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
